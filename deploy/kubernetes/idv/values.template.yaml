host: <YOUR HOST HERE>

pods:
  image:
    tag: latest
  environment: prod

# create this storage class if storage.useExisting is false or not set
storage:
  provisioner: # cloud-specific storage provisioner e.g. kubernetes.io/aws-ebs for AWS
  parameters:
#    Cloud-specific values. See below for AWS
#    type: gp2
#    zones: us-east-1a, us-east-1b
