{{- if .Values.analytics.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: analytics-secret
type: Opaque
data:
{{ (.Files.Glob "config/admininterface/secrets/*").AsConfig | b64enc | indent 2 }}
{{- end}}
