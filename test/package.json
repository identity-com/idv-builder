{
  "name": "idv-builder-tests",
  "version": "1.0.0",
  "description": "Idv Builder tests",
  "license": "MIT",
  "scripts": {
    "pretest": "sh scripts/startIdv.sh",
    "test": "mocha --timeout 60000 --recursive e2e --require mocha-steps"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "cross-env": "^7.0.2",
    "mocha": "^8.0.1",
    "mocha-steps": "^1.3.0",
    "request": "^2.88.2",
    "request-promise-native": "^1.0.8"
  }
}
